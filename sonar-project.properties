# SonarCloud analysis is executed via the Maven plugin (`mvn sonar:sonar`).
# In that mode, Sonar configuration is read from the Maven project (`pom.xml`),
# not from this file. Keep `pom.xml` as the single source of truth to avoid
# configuration drift between duplicated Sonar settings.